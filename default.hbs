<html lang="{{@site.locale}}">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{meta_title}}</title>

    <link rel="stylesheet" type="text/css" href="{{asset 'css/screen.css'}}" />

    {{ghost_head}}
  </head>
  <body class="{{body_class}}">
    <div class="site-wrapper">

      <header class="site-header-global">
        <div class="inner-container header-layout">
          <div class="header-logo-container">
            <a class="site-nav-logo" href="{{@site.url}}">

              {{#if @site.logo}}
                <img
                  src="{{@site.logo}}"
                  alt="{{@site.title}}"
                  class="logo-image"
                />
              {{else}}
                {{@site.title}}
              {{/if}}
            </a>
          </div>

          <div class="header-nav-container">
            {{#if @site.navigation}}
              {{navigation}}
            {{/if}}
          </div>
        </div>
        <div class="header-divider"></div>
      </header>

      {{{body}}}

      <footer class="site-footer-global">
        <div class="inner-container">
          <section class="copyright">
            <span>All the illustration on the website are done by
              <a
                href="https://www.linkedin.com/in/bansi-sharma-977004133/"
                target="_blank"
                rel="noopener"
              >Bansi</a></span>
            <br />
            <a href="{{@site.url}}">{{@site.title}}</a>
            &copy;
            {{date format="YYYY"}}. All rights reserved.
            <br />
            <span>Contact us:
              <a
                href="mailto:learningwalamihir@gmail.com"
              >learningwalamihir@gmail.com</a>
              | Mihir - 09537068736</span>

          </section>
          <nav class="site-footer-nav">
            {{#if @site.secondary_navigation}}
              {{navigation type="secondary"}}
            {{else}}
              <a href="{{@site.url}}">Latest Posts</a>
              {{#if @site.facebook}}<a
                  href="{{social_url type="facebook"}}"
                  target="_blank"
                  rel="noopener"
                >Facebook</a>{{/if}}
              {{#if @site.twitter}}<a
                  href="{{social_url type="twitter"}}"
                  target="_blank"
                  rel="noopener"
                >Twitter</a>{{/if}}
            {{/if}}
            <a
              href="https://ghost.org"
              target="_blank"
              rel="noopener"
              class="powered-by"
            >Powered by Ghost</a>
          </nav>
        </div>
      </footer>

    </div>

    {{ghost_foot}}
  </body>
</html>