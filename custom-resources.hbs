{{!< default}}

<main id="main-content" class="site-main">
  <div class="inner-container resources-inner">
    {{#post}}
      <header class="page-header">
        <h1 class="page-title">{{title}}</h1>
        
        {{#if custom_excerpt}}
          <p class="page-description">{{custom_excerpt}}</p>
        {{/if}}
      </header>

      <div class="page-content resources-page-content">
        {{content}}
      </div>
    {{/post}}

    <section class="resources-section">
      <div class="resources-grid">
        {{#get "posts" filter="tag:hash-resource" limit="100"}}
          {{#foreach posts}}
            <article class="resource-card">
              {{!-- We use the custom_excerpt to store the file link; if not present, we fall back to the post URL --}}
              <a 
                class="resource-link"
                href="{{#if custom_excerpt}}{{custom_excerpt}}{{else}}{{url}}{{/if}}" 
                target="_blank" 
                rel="noopener noreferrer" 
                {{#if custom_excerpt}}download{{/if}}
              >
                <div class="resource-image-container">
                  {{#if feature_image}}
                    <img
                      src="{{img_url feature_image size="m"}}"
                      alt="{{title}}"
                      class="resource-image"
                      loading="lazy"
                    />
                  {{else}}
                    <div class="resource-placeholder">
                      <svg width="48" height="48" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path></svg>
                    </div>
                  {{/if}}
                </div>
                <div class="resource-content">
                  <h3 class="resource-title">{{title}}</h3>
                  <div class="resource-meta">
                    <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                    Download
                  </div>
                </div>
              </a>
            </article>
          {{else}}
            <p class="section-empty-note">New resources will appear here soon.</p>
          {{/foreach}}
        {{/get}}
      </div>
    </section>
  </div>
</main>